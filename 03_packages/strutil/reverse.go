package strutil

func Reverse(s string) string {	// it has to be 'R'everse to be available outside of the package
	runes := []rune(s)
	for i, j := 0, len(runes)-1; i < j; i, j = i+1, j-1 {
		runes[i], runes[j] = runes[j], runes[i]
	}
	return string(runes)
}
